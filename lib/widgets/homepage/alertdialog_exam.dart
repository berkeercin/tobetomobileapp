import 'package:flutter/material.dart';

class ExamAlertDialog extends StatelessWidget {
  const ExamAlertDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
